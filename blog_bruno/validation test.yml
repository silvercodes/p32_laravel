info:
  name: validation test
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost/validation
  body:
    type: json
    data: |-
      {
        "user": {
          
        },
        "posts": [
          
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
